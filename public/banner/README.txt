LOCAL BANNER FALLBACKS

If a member has no Discord banner, the profile automatically tries:
  /banner/<profilePath>.gif
then:
  /banner/<profilePath>.png

Examples:
  /banner/DEN.gif
  /banner/VINCE.png

Discord/Lanyard banner data is always preferred when available.
